In this task, the user needs to implement several functions. 
You can verify this in user mode – the content of the `Main.kt` file will propagate from the previous step,
whereas `InvisibleFile.kt` will change.
