This is an example of a theory lesson.

<div class="hint" title="What is the difference between visible and invisible files?">
  
You can use hint blocks to provide additional explanations for students. 
Be sure to leave a blank line before the content inside the hint block to ensure Markdown formatting renders correctly.

Invisible files are hidden in student mode. 
You can use them to define helper functions for your course without exposing them to students.

To mark a file as invisible, set `is_visible: false` for the corresponding file in the [task-info](./task-info.yaml) configuration file.
</div>

Theory tasks do not contain any tests.
